<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ApexFitness" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.ApexFitness" parent="Base.Theme.ApexFitness" />

    <!-- Splash screen shown while the app starts -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.ApexFitness</item>
    </style>
</resources>
